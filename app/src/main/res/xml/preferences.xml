<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

	<PreferenceCategory
		android:title="Paramètres du serveur BDD">

		<EditTextPreference
            android:key="PREFKEY_HOSTNAME"
			android:id="@+id/pref_bdd_hostname"
			android:title="Nom d'hôte"
			android:summary="Entrez le nom d'hôte du serveur SQL Express"
			android:defaultValue="@string/default_pref_bdd_hostname" />

		<EditTextPreference
            android:key="PREFKEY_PORT"
			android:id="@+id/pref_bdd_port"
			android:title="Port"
			android:summary="Entrez le numéro de port du serveur SQL Express"
			android:defaultValue="@string/default_pref_bdd_port" />

		<EditTextPreference
            android:key="PREFKEY_USERNAME"
			android:id="@+id/pref_bdd_username"
			android:title="Nom d'utilisateur"
			android:summary="Entrez le nom du compte autorisé"
			android:defaultValue="@string/default_pref_bdd_username" />

		<EditTextPreference
            android:key="PREFKEY_PASSWORD"
			android:id="@+id/pref_bdd_password"
			android:title="Mot de passe"
			android:summary="Entrez le mot de passe de connexion"
			android:defaultValue="@string/default_pref_bdd_password" />

	</PreferenceCategory>

	<PreferenceCategory
		android:title="Paramètres du serveur SNMP">

		<EditTextPreference
			android:id="@+id/pref_snmp_hostname"
			android:title="Nom d'hôte"
			android:summary="Entez le nom d'hôte du serveur SNMP"
			android:defaultValue="@string/default_pref_snmp_hostname" />

		<EditTextPreference
			android:id="@+id/pref_snmp_port"
			android:title="Port SNMP"
			android:summary="Entrez le numéro de port du serveur SNMP"
			android:defaultValue="@string/default_pref_snmp_port" />

		<EditTextPreference
			android:id="@+id/pref_snmp_password"
			android:title="Communauté SNMP en lecture"
			android:summary="Entrez le nom de la communauté SNMP autorisée en lecture"
			android:defaultValue="@string/default_pref_snmp_password" />

	</PreferenceCategory>

	<PreferenceCategory
		android:title="Paramètres de la sonde température SNMP">

		<EditTextPreference
			android:id="@+id/pref_snmp_temp_hostname"
			android:title="Nom d'hôte"
			android:summary="Entez le nom d'hôte de la sonde SNMP"
			android:defaultValue="@string/default_pref_snmp_temp_hostname" />

		<EditTextPreference
			android:id="@+id/pref_snmp_temp_port"
			android:title="Port SNMP"
			android:summary="Entrez le numéro de port de la sonde SNMP"
			android:defaultValue="@string/default_pref_snmp_temp_port" />

		<EditTextPreference
			android:id="@+id/pref_snmp_temp_password"
			android:title="Communauté SNMP en lecture"
			android:summary="Entrez le nom de la communauté SNMP autorisée en lecture sur la sonde"
			android:defaultValue="@string/default_pref_snmp_temp_password" />

	</PreferenceCategory>

    <PreferenceCategory
        android:title="Divers">

        <EditTextPreference
            android:key="PREFKEY_NB_VALUES"
            android:id="@+id/pref_number_values"
            android:inputType="numberDecimal"
            android:title="Nombre de valeurs"
            android:summary="Entez le nombre de valeurs à retourner"
            android:defaultValue="@string/default_number_values" />

    </PreferenceCategory>

</PreferenceScreen>